scalar Date

type Post {
  id: String!
  imageUrl: String
  caption: String
  createdAt: Date!
}

type PostPayload {
  isOk: Boolean!
  post: Post
  errors: [Error]
}

type Query {
  getPost(id: String!): Post
  getUserPosts(userId: String!): [Post]!
}

type Mutation {
  createPost(imageUrl: String, caption: String!): PostPayload!
  updatePost(imageUrl: String, caption: String, postId: Int!): PostPayload!
  deletePost(postId: String!): PostPayload!
}
